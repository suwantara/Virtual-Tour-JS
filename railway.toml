[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
# Target khusus Railway: single container nginx + php-fpm via supervisor
dockerBuildTarget = "railway"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
