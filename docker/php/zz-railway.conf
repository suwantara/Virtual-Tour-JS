[www]
; Railway runs as a single container as root.
; Override the default www-data pool user so php-fpm workers
; can write to storage/framework/views when compiling Blade components.
user = root
group = root
listen.owner = root
listen.group = root
